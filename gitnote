git 版本控制 分布式管理系统

Working Directory     git add   Stage(暂存区)  git commit   Local Repository     git push           Remote Repository
                     --------->             -------------->                   ---------------->
				git pull	or	git clone
                      <--------------------------------------------------------- 
由working directory 上传到remote repository的过程
                                                       
一、提交到Remote Repository的过程：
1. 初始化设置：
一台设备只需要初始化设置一次
git config --global user.name "User_name"   以什么身份提交
git config --global user.email "GitHub绑定的邮箱"   需要是github的邮箱，不只是身份还关系到github的数据
git config --global credential.helper store  存储配置 记住账号密码（如果用SSH或者图形化界面登陆则不需要这个）
Git 有三种登录方式（HTTPS+密码、HTTPS+Token、SSH密钥）

2.创建仓库
git init <project_name>
<project name>可选 意为在当前文件夹下创建一个名为 project name 的文件夹并且让他变成一个git仓库
不加意为将当前文件夹变为一个git仓库
会生成.git文件 最好不要修改 一个仓库只需要git init一次

3.添加和提交
git add <file>  添加一个文件到暂存区 git add .表示添加当前目录以及其子目录下所有文件全都添加到暂存区
若只想提交部分文件，可以在<file>处填想提交的文件名 多个文件空格隔开
通配符号 git add *.py 提交所有python文件
git add src/   添加src文件夹内所有文件
git add !*.log  除了.log文件 其他都添加

git commit  将暂存区的内容打包生成一个永久性的版本快照 并保存到本地仓库里 生成一个commit id
git commit 会弹出一个文本编辑器 让我们在其中写详细的提交说明 适合写长说明
git commit -m "提交说明"    直接在后面跟简短的说明 
git commit -a -m "提交说明"   跳过git add 直接把所有已经修改且被git 跟踪过的文件提交 但不会包含新创建的文件
.git文件夹就是本地仓库


4.本地仓库上传到远程仓库 最好别加README.md用于描述仓库内容  后续可在github上加（https方法）
先在github上创建一个仓库
会生成HTTPS和SSH
HTTPS: https://github.com/MingxianLi2006/51-Project.git
SSH: git@github.com:MingxianLi2006/51-Project.git


git remote add origin https://github.com/用户名/仓库名.git  关联远程仓库
git push -u origin main 
-u是--set--upstream 把本地的main 分支和远程的main分支绑定 第一次推送必须加 之后git push 就行
三种常见报错
![rejrcted]  (远程有更新)
说明同事或自己在其他地方推送过新的代码
先拉去再推送
git pull origin main
git push

fatal:No configured push destination 没关联远程仓库
忘记git remote add了

弹窗要输入账号密码
用户名填github账号 密码填Token 个人访问令牌  Token需要在github设置里生成一个
Tokens可以是Tokens(classic)或者Fine-grained Token 后者可以控制权限控制的很细
git remote -v检查远程地址对不对

git push -v查看推送进度 
-v是verbose详细模式

5.SSH方法
SSH key每一个都永久有效 同时可用
第一步 生成SSH Key
ssh-keygen -t ed25519 -C "邮箱"
ssh-keygen  used to generate key
-t  type指定密钥类型
ed25519 一种加密算法
-C comment备注一下
第二步 把公钥添加到github账号上
cat ~/.ssh/id_ed25519.pub
去github setting里面找SSH and GPG keys-> New SSH Key  粘贴进去
第三步 用SSH地址克隆或修改远程地址
如果已经使用了HTTPS地址 改成SSH地址：
git remote set-url origin git@github.com:用户名/仓库名.git
新项目
git clone git@github.com:用户名/仓库名.git
第四步 正常提交
git add .
git commit -m "message"
git push

说明：公钥id_ed25519.pub   私钥id_ed25519
使用公钥配置github 
推送代码时公钥和私钥配对

二、从Remote Repository克隆下来项目(ssh方法)
0.生成ssh密钥对 每台设备做一次 公钥粘贴到github
确认ssh 连接 ssh -T git@github.com
1.找到项目的ssh地址
找到仓库 code 找到ssh标签 复制git@github.com:用户名/仓库名.git
cd到要存放项目的父目录
2.执行克隆命令
git clone git@github.com:用户名/仓库名.git
执行后会自动创建和仓库同名的文件夹 完整下载 自动初始化（生成.git文件）自动切到默认分支
克隆后的工作流程（有权限）：
cd <project_file> 进入项目文件夹
#修改代码
git add .
git commit -m "修改的内容"
git push #直接推送 clone已经配好了地址

只克隆的话 https更方便
git clone https://github.com/用户名/仓库名.git
可在最后加一个新名字用于在本地重命名文件夹

3.不同设备提交不同内容到同一仓库，第二台设备该怎么做：
0.配ssh密钥
1.在第二台设备的终端里进入要存放项目的文件夹
git clone git@github.com:用户名/仓库名.git
2.克隆成功后会多出一个和仓库同名的文件夹 cd 仓库名
git add .
git commit -m "message"
git push
第一台电脑git pull拉取最新内容
clone只在新电脑第一次下载时候用一次 之后就是pull和push
必须在相应的working directory pull和push 
用git remote -v可查看当前仓库关联的远程地址
pwd显示当前路径

4.分支操作:
命令			作用
git branch		查看所有本地分支 当前分支带有*
git brnach 分支名	创建新分支
git switch 分支名
git checkout 分支名	切换到某个分支
git switch -c 分支名	创建并立即切换到新分支（最常用）
git merge 分支名	将指定分支合并到当前分支
git branch -d 分支名	删除分支（合并后才允许删除）

eg.
git branch 查看当前分支 可能输出 *main
git switch -c dev
echo "新功能" > feature.txt
git add.
git commit -m "在dev分支上添加了新功能"
git switch main  切换回main
git merge dev   把dev合并到main
git branch -d dev   删除dev分支 已合并

Pull Request (PR)
把某个分支的改动请求合并到另一个分支
PR就是把这段流程里的git merge dev 从本地命令搬到github上 让其他人先审查代码 再合并
：我在本地建了个分支并推了上去 现在在网页上请求你把我的分支合并到主分支
本地分支操作		PR对应操作
git switch -c feature	在本地创建分支
git add. + git commit	改代码 提交
git push origin feature	把分支推上github
git merge feature	PR网页上点击 Merge pull request

eg.
---

## 场景设定

- **项目仓库**：`https://github.com/老板/awesome-project.git`
- **代码提交者**：你（没有直接 push 权限，需要提 PR）
- **代码审查/合并者**：老板（有仓库管理权限，负责审查和合并）

---

## 第一幕：代码提交者（你）的操作

### 第 1 步：Fork 项目到自己的账号

去 GitHub 上 `老板/awesome-project` 仓库页面，点击右上角的 **Fork**，把项目复制一份到 `你的用户名/awesome-project`。
gitee叫派生
### 第 2 步：克隆你自己 Fork 的仓库

```bash
git clone git@github.com:你的用户名/awesome-project.git
cd awesome-project
```

### 第 3 步：配置上游仓库（方便以后同步老板的更新）

```bash
git remote add upstream git@github.com:老板/awesome-project.git
```

验证：
```bash
git remote -v
# origin    git@github.com:你的用户名/awesome-project.git (fetch)
# origin    git@github.com:你的用户名/awesome-project.git (push)
# upstream  git@github.com:老板/awesome-project.git (fetch)
# upstream  git@github.com:老板/awesome-project.git (push)
```

### 第 4 步：创建新分支并开发

```bash
git switch -c feature/add-login-page
# 改代码... 比如新建 login.html，修改 app.css 等
```

### 第 5 步：提交并推送到自己的 Fork

```bash
git add .
git commit -m "feat: 添加登录页面"
git push origin feature/add-login-page
```

### 第 6 步：在 GitHub 上创建 PR

1. 去你 Fork 的仓库页面：`github.com/你的用户名/awesome-project`
2. 刷新后，会出现 **"Compare & pull request"** 按钮，点它
3. 如果没有，点顶部 **Pull requests** → **New pull request** → 选 `head repository` 为自己的仓库和 `feature/add-login-page` 分支，`base repository` 选 `老板/awesome-project` 的 `main` 分支
4. 填写 PR 标题和描述（写清楚改了啥、为什么改）
5. 点 **Create pull request**

### 第 7 步：根据审查意见修改（可能多轮）

老板可能提出修改意见，你在本地继续改：

```bash
# 继续改代码...
git add .
git commit -m "fix: 按 review 意见调整样式"
git push origin feature/add-login-page   # PR 会自动更新，不需要重新创建
```

---

## 第二幕：代码审查/合并者（老板）的操作

### 第 1 步：查看 PR

1. 去 `老板/awesome-project` 仓库页面
2. 点顶部 **Pull requests** 标签
3. 看到你提交的 PR（标题："添加登录页面"），点进去

### 第 2 步：审查代码

- 点击 **Files changed** 标签，逐行看改动
- 有问题的地方点 **+** 号添加评论
- 全部看完后，在顶部点 **Review changes**：
  - **Comment**：只评论，不通过
  - **Approve**：通过审查
  - **Request changes**：要求修改

### 第 3 步：本地拉取 PR 分支测试（可选，但推荐）

如果老板想把代码拉到本地测试一下再合并：

```bash
# 老板在自己的电脑上
git fetch origin pull/PR编号/head:pr-分支名
git switch pr-分支名
# 本地测试代码...
```

### 第 4 步：合并 PR

审查通过且测试没问题后，在 PR 页面底部：

- 点击 **Merge pull request** 按钮
- 点击 **Confirm merge**
- 合并成功后，点击 **Delete branch**（删除远程分支，保持仓库整洁）

### 第 5 步：拉取合并后的最新代码（老板自己更新）

```bash
git switch main
git pull origin main
```

---

## 第三幕：代码提交者（你）同步老板的最新代码

PR 合并后，你本地仓库需要同步老板的最新改动：

```bash
git switch main
git pull upstream main          # 从老板的仓库拉取最新代码（包括刚才合并的 PR）
git push origin main            # 更新你自己 Fork 的 main 分支
git branch -d feature/add-login-page   # 删除本地开发分支
```

---

## 完整流程图

```
代码提交者（你）                         代码审查/合并者（老板）
      │                                        │
      ├─ Fork 项目到自己的账号                  │
      ├─ git clone 自己的 Fork                  │
      ├─ git remote add upstream               │
      ├─ git switch -c feature分支              │
      ├─ 改代码 + commit                        │
      ├─ git push origin feature分支            │
      ├─ 在 GitHub 网页上创建 PR ────────────────┤
      │                                        ├─ 看到 PR
      │                                        ├─ 审查代码（Files changed）
      │                                        ├─ 提修改意见
      ├─ 根据意见修改 + commit + push ────────────┤
      │                                        ├─ 再次审查通过
      │                                        ├─ 点击 Merge pull request
      │                                        ├─ 合并成功 ✓
      ├─ git pull upstream main（同步）          │
      ├─ git push origin main                  │
      ├─ git branch -d feature分支              │
      ▼                                        ▼
```

---

## 关键命令速查表

| 操作 | 命令（提交者） | 命令（审查者） |
|------|-------------|--------------|
| Fork 项目 | GitHub 网页点 Fork | - |
| 克隆 | `git clone git@github.com:你的用户名/仓库.git` | `git clone git@github.com:老板/仓库.git` |
| 添加上游 | `git remote add upstream 老板地址` | - |
| 新建分支 | `git switch -c 分支名` | - |
| 推送分支 | `git push origin 分支名` | - |
| 拉取测试 | - | `git fetch origin pull/编号/head:pr-分支` |
| 合并 PR | - | GitHub 网页点 Merge |
| 同步更新 | `git pull upstream main` | `git pull origin main` |

---

## 遇到冲突怎么办？

如果老板的 `main` 分支在你提 PR 之后有新提交，合并时可能冲突。提交者需要：

```bash
git switch main
git pull upstream main          # 拉取老板最新代码
git switch feature/add-login-page
git merge main                  # 把 main 合并到 feature，解决冲突
# 解决完冲突后...
git add .
git commit -m "merge: 解决与 main 的冲突"
git push origin feature/add-login-page
```

PR 会自动更新，老板重新审查后合并。

---

**你现在可以拿自己现有的仓库练手了：**
1. 创建一个新分支
2. 改点东西
3. 推送到 GitHub
4. 在网页上创建 PR（自己合并自己的，练手）

文件状态：
Modified 已修改但是没保存到暂存区的文件
Staged 修改后已经保存到暂存区的文件
Committed 把暂存区的文件提交到本地仓库后的状态
特殊文件:
.git
.gitignore 用于存储不被git管理的文件

查看历史提交


**恭喜你！** 🎉 你已经从一个 Git 小白，成长到能独立完成 **克隆、分支、提交、推送、PR 全流程** 的水平了。这是很多开发者用了半年才摸索出来的完整闭环。

不过，如果你想让自己的 Git 技能再上一个台阶，可以从下面这 **5 个方向** 挑选你感兴趣的继续深入。我把它们按 **"遇到频率"** 和 **"重要性"** 排了个序：

---

### 🚀 阶段一：必知必会（工作上几乎天天遇到）

1. **解决冲突 (Merge Conflict)** —— 最最重要的保命技能
   - 你和同事改了同一文件的同一行，`git pull` 时报错 "merge conflict"。
   - 需要学：怎么看懂冲突标记（`<<<<<<<`、`=======`、`>>>>>>>`），怎么手动解决，怎么 `add` + `commit` 完成合并。

2. **`git log` 和 `git diff`** —— 看历史和看改动
   - `git log --oneline --graph`：用最直观的方式看提交树。
   - `git diff`：commit 前看一眼改了啥，防止手滑把调试代码也交上去。

3. **`.gitignore`** —— 不让 Git 管垃圾文件
   - 比如 `node_modules/`、`.env`、`*.log`、IDE 配置文件夹。
   - 学一下怎么在项目根目录建这个文件，把不想上传的东西写进去。

---

### 🛠️ 阶段二：修修补补（翻车时的救命稻草）

4. **撤销操作 (Undo)**
   - `git reset --soft HEAD~1`：撤销 commit，但保留修改（重新提交）。
   - `git reset --hard HEAD~1`：彻底丢弃最后一次 commit（**危险，慎用**）。
   - `git restore 文件名`：丢弃工作区的修改（回到上一次 commit 的状态）。

5. **`git stash`** —— 临时藏起未提交的改动
   - 你改到一半，突然要切分支修 bug，但又不想 commit。`git stash` 暂存起来，修完 bug 后 `git stash pop` 恢复。

---

### 🧠 阶段三：理解本质（懂了之后不再慌）

6. **`git fetch` 和 `git pull` 的关系**
   - `git pull` = `git fetch` + `git merge`。
   - 你其实可以先用 `git fetch` 看看远程有什么更新，再决定要不要合并。

7. **HEAD、工作区、暂存区、本地仓库、远程仓库**
   - 你现在是“会用”，但搞清楚这 5 个东西的关系，能解决 90% 的玄学报错。

8. **`git rebase` 是什么？和 `merge` 有什么区别？**
   - merge 保留分支历史，rebase 整理成一条直线。团队里如果是“线性历史”党，会要求你用 rebase。

---

### 🌍 阶段四：团队协作规范（非技术，但重要）

9. **Commit Message 规范**
   - 比如 Angular 规范：`feat: 添加登录功能`、`fix: 修复登录超时 bug`、`docs: 更新 README`。
   - 好处：一目了然，自动生成 Changelog。

10. **分支管理策略（Git Flow / GitHub Flow）**
    - 简单项目用 GitHub Flow：`main` 永远可发布，新功能开分支，PR 合并。
    - 大项目用 Git Flow：有 `develop`、`feature`、`release`、`hotfix` 分支。

---

### 🧪 阶段五：进阶（炫技 + 提效）

11. **给开源项目提 PR 的完整流程**（你现在已经会了，但可以实战一次）

12. **Git 钩子 (Hooks)**：在 commit 前自动运行测试、格式化代码。

13. **Git LFS**：存大文件（如图片、视频、模型文件）不撑爆仓库。

---

### 💡 我的建议（按顺序来）

1. **先搞定 "解决冲突" + `.gitignore`** —— 这两个是你工作第一周一定会遇到的。
2. 然后掌握 `git log` 和 `git diff` —— 帮你少犯低级错误。
3. 等有自信了，再看 `git reset` 和 `git stash` —— 专治各种手滑。
4. 其他高级操作（rebase、hooks、LFS）遇到了再学，不用提前背。

---

你现在想先深入了解哪个？比如：
- **"怎么看冲突并解决？"**
- **".gitignore 怎么写？"**
- **"git stash 怎么用？"**
- 还是想直接实战一个：**"用你现在的项目跑一次 PR 全流程"**？

告诉我你想走的下一站，我带你继续。😊

**这个操作很简单，但是非常重要！** 忘记很正常，记住几个最常用的命令就行了。

---

## 最常用的查看历史命令

### 1. `git log` —— 标准查看方式

```bash
git log
```

输出会显示：
- 每次提交的 **commit ID**（一长串哈希值）
- **作者**（Author）
- **日期**（Date）
- **提交信息**（commit message）

按 `q` 退出查看界面。

---

### 2. `git log --oneline` —— 最精简（强烈推荐）

```bash
git log --oneline
```

输出示例：
```
a1b2c3d feat: 添加登录页面
e4f5g6h fix: 修复登录超时 bug
i7j8k9l docs: 更新 README
```

**优点：**
- 每条提交只显示 7 位 commit ID + 提交信息
- 干净清爽，一目了然
- 最常用，没有之一

---

### 3. `git log --oneline --graph` —— 看分支分叉情况

```bash
git log --oneline --graph
```

输出示例：
```
* a1b2c3d feat: 添加登录页面 (HEAD -> feature/login)
* e4f5g6h fix: 修复登录超时 bug
| * i7j8k9l docs: 更新 README (main)
|/  
* m0n1o2p initial commit
```

**优点：**
- 能看到分支从哪里分叉、在哪里合并
- 对理解分支走向非常有帮助

---

### 4. `git log --oneline --graph --all` —— 看所有分支（包括远程）

```bash
git log --oneline --graph --all
```

输出会显示本地和远程所有分支的状态。

---

### 5. `git log -p` —— 看每次提交具体改了哪些代码

```bash
git log -p
```
或者只看最近 2 次：
```bash
git log -p -2
```

**优点：**
- 能看到每次提交的具体代码改动（+ 号和 - 号）
- 适合审查某次提交到底改了哪些行

---

### 6. `git log --author="名字"` —— 只看某个人的提交

```bash
git log --oneline --author="张三"
```

---

### 7. `git log --since="2024-01-01"` —— 按时间范围筛选

```bash
git log --oneline --since="2024-01-01" --until="2024-12-31"
```

---

## 速查表（存下来，以后忘了就翻）

| 命令 | 作用 |
|------|------|
| `git log` | 标准历史列表 |
| `git log --oneline` | 精简版（**最常用**） |
| `git log --oneline --graph` | 带分支图 |
| `git log --oneline --graph --all` | 所有分支都显示 |
| `git log -p` | 显示每次提交的具体代码改动 |
| `git log --author="名字"` | 只看某人的提交 |
| `git log --since="日期"` | 按时间筛选 |

---

## 退出历史查看界面

如果你执行 `git log` 后卡在一个带 `:` 的界面里，按键盘上的 **`q`** 键就能退出。

---

## 你现在可以试试

在你的项目目录下执行：
```bash
git log --oneline --graph --all
```

看看你的历史提交是什么样的。如果结果显示有很多提交，说明你已经有不少记录了；如果只显示几条，说明你的项目刚开始不久。

**执行完把输出告诉我，我帮你分析一下你的提交历史！** 😊

**答案是：可以，但有条件。**

关键在于**你能不能访问到这个仓库的 `.git` 文件夹**。只要你能拿到这个仓库的本地副本，基本上就能看历史。

---

### 1. 你自己的本地仓库（100% 可以）
你自己 `git clone` 下来的项目，或者 `git init` 创建的项目，**随时可以**：
```bash
git log --oneline
```
因为 `.git` 文件夹就在你电脑上，随便看。

---

### 2. 别人的公开仓库（可以）
只要是 GitHub/Gitee 上的**公开项目（Public）**，你虽然不能直接 `push`，但你可以 `clone` 下来，然后**随便看历史**：
```bash
git clone https://github.com/别人/仓库.git
cd 仓库
git log --oneline
```
因为公开仓库的 `.git` 是允许被下载的。你可以看到这个项目从第一行代码到现在的所有提交记录。

---

### 3. 别人的私有仓库（不可以，除非你被授权）
如果是**私有仓库（Private）**，你没有权限的话：
- 网页上看不到
- `git clone` 会被拒绝（`403` 或 `Permission denied`）

如果你被老板添加为 **Collaborator（协作者）**，或者加入了对应的团队，那你就有权限克隆并查看历史了。

---

### 4. 只有代码（没有 .git）的文件夹（不可以）
如果只是从网页上下载了 **ZIP 压缩包**，或者复制了别人的源代码但**没有 `.git` 文件夹**，那就看不了。因为历史记录存在 `.git` 里，不在代码文件里。

---

### 怎么判断一个项目我能不能看历史？

打开这个项目的 GitHub 页面：
- 如果项目是 **Public** → 你能看
- 如果项目是 **Private**，并且你能正常打开文件列表 → 你能看
- 如果项目是 **Private**，但你打开页面时显示 `404` 或要求登录 → 你看不了

---

### 总结一下

| 仓库状态 | 能不能看历史 |
|---------|------------|
| 你电脑上有这个仓库（`git clone` 过） | ✅ 一定能 |
| GitHub 上的公开仓库 | ✅ 能（`clone` 下来就能看） |
| GitHub 上的私有仓库，你有权限 | ✅ 能 |
| GitHub 上的私有仓库，你没权限 | ❌ 不能 |
| 只有代码文件，没有 `.git` 文件夹 | ❌ 不能 |

---

**你现在是想看哪个仓库的历史？** 是你自己电脑上已有的，还是 GitHub 上别人的项目？告诉我具体情况，我教你对应的操作。😊



关于ssh:
本身是远程登录协议 用于安全连接远程服务器
具有两个核心能力：
远程登陆shell+安全传输数据
git使用了后者 用于验证身份和数据传输 git可视为一个专用系统账号 但是权限有限
